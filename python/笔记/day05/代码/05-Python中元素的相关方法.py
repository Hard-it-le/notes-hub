# 1、元组中某个元素的访问
nums = (10, 20, 30)
print(nums[1])

# 2、查找20在元组nums中出现的位置
print(nums.index(20))

# 3、统计元素在元组中出现的次数
nums = (10, 20, 30, 50, 30)
print(nums.count(30))

# 4、求元组的长度（里面有多少个元素）
print(len(nums))